## 2026-04-19 - US-008
- Implemented: Rewrote apps/web/app/globals.css around the warm editorial token system from `wathba auditor/styles.css`. Semantic layer now defines surfaces (--background, --surface, --surface-variant, --surface-sunken), text (--foreground, --muted-foreground, --soft-foreground), borders (--border, --border-strong), primary family (--primary, --primary-hover, --primary-soft, --primary-foreground), --secondary + secondary-foreground, and full semantic state colors (--destructive, --success, --warning, --info). Dark theme is a deliberate warm counterpart (not an inversion) and activates under both `.dark` and `[data-theme="dark"]` so US-007 can use data attributes without changing tokens. Shadcn-compat aliases (--card, --popover, --muted, --accent, --input, --ring, --sidebar-*, --chart-*) are now derived from the semantic tokens so existing shadcn primitives stay in-system. Expanded @theme inline so Tailwind generates utilities for every semantic token: `bg-surface`, `bg-surface-variant`, `bg-surface-sunken`, `text-muted-foreground`, `text-soft-foreground`, `border-border-strong`, `bg-primary-soft`, `text-success`, `bg-info`, etc. Added shared motion token --motion-step (200ms cubic-bezier). Added .wathba-surface/.wathba-surface-variant/.wathba-surface-sunken/.wathba-elevated utilities for raw CSS consumers. Documented the token taxonomy with a block comment at the top of globals.css and a contributor note in apps/web/AGENTS.md (consume tokens, never hex).
- Files changed: apps/web/app/globals.css, apps/web/AGENTS.md, ralph/prd.json, ralph/progress.txt
- Checks run: pnpm verify — all green (generate, doctor:react, schema typecheck, web typecheck, 5 vitest tests, static build emits /en, /ar, /en/compare, /ar/compare, /en/skills, /ar/skills, /en/skills/saudi-zatca-phase2, /ar/skills/saudi-zatca-phase2, /en/generate, /ar/generate).
- Notes for future work: US-010 (primary swatches ink/olive/slate) should layer `:root[data-primary="…"]` selectors in globals.css that override --primary, --primary-hover, --primary-soft, --primary-foreground only — other semantic tokens stay stable. US-009 (typography) should set --font-latin + --font-arabic via next/font in the locale layout; the body/display mapping is already wired. US-007 may toggle data-theme at runtime; tokens already respond. The old hard-coded --accent=#e4d5c4 light value is intentional (warm mid-tone) — convert to a surface-derived token if a future primitive needs accent parity. `.dark` class still works for any legacy shadcn demos; prefer `[data-theme="dark"]` going forward.
---

## 2026-04-19 - US-005
- Implemented: Rewrote AppShell to match the Wathba Skills editorial shell — sticky top nav with brand mark ("W"), brand + alpha badge, nav links for Overview/Skills/Generate/Compare, LocaleSwitch, GitHub icon button, and a primary "Generate skill pack" CTA. Footer now carries MIT License, Disclaimer, Contributing, and Repository links alongside the engineering-guidance disclaimer. All labels flow through a new Shell namespace in messages/en.json and messages/ar.json, so the nav reverses spatially under RTL via flex + logical ms-* classes. Added a stub /[locale]/compare route (section="compare") so the nav link is not broken — US-042 will replace it with the real bilingual parity canvas. Widened AppShell section union to include "compare". Updated layout metadata to "Wathba Skills". GitHub icon uses an inline SVG because lucide-react no longer exports Github.
- Files changed: apps/web/components/app-shell.tsx, apps/web/app/[locale]/compare/page.tsx (new), apps/web/app/[locale]/layout.tsx, apps/web/messages/en.json, apps/web/messages/ar.json, ralph/prd.json, ralph/progress.txt
- Checks run: pnpm verify — all green (generate, doctor:react, schema typecheck, web typecheck, 5 vitest tests, static build emits /en, /ar plus /compare, /skills, /skills/saudi-zatca-phase2, /generate for both locales).
- Notes for future work: Compare route is a placeholder that renders two cards with lang/dir set — US-042 should replace it with the real side-by-side frames pulling from the same data source. Footer links point to a REPO_URL constant at github.com/wathba-skills/wathba-skills plus expected paths (LICENSE, DISCLAIMER.md, CONTRIBUTING.md) — US-050 ships those docs; until then links 404 remotely. AppShell is now an async server component (awaits getTranslator); existing pages that render it without awaiting JSX work fine because RSC handles async children. Primary CTA is hidden on <sm screens to avoid crowding; nav itself still wraps.
---

## 2026-04-19 - US-004
- Implemented: Locale-aware routing already existed via app/[locale]/ with dynamicParams=false + generateStaticParams returning only en/ar; html element already got lang + dir from getDirection(). Added `swapLocaleInPath` helper in lib/i18n.ts and extracted LocaleSwitch to a client component that uses usePathname() so switching EN<->AR preserves the current screen (was always jumping to /<locale>). Unit tests cover isLocale, getDirection, and swapLocaleInPath edge cases.
- Files changed: apps/web/lib/i18n.ts, apps/web/components/locale-switch.tsx (new), apps/web/components/app-shell.tsx, apps/web/tests/unit/i18n.test.ts (new), ralph/prd.json, ralph/progress.txt
- Checks run: pnpm verify — all green (generate, doctor:react, schema typecheck, web typecheck, 5 vitest tests, static build emits /en, /ar, /en/skills, /ar/skills, /en/skills/saudi-zatca-phase2, /ar/skills/saudi-zatca-phase2, /en/generate, /ar/generate).
- Notes for future work: LocaleSwitch is now a client component so whatever page includes AppShell gets a client boundary for the switcher only — server components upstream still render statically. When US-006 lands (persist route in localStorage), locale swap may also want to sync the stored `wathbaSkills.route` — today it just relies on URL pathname. No root app/layout.tsx at apps/web/app — only [locale]/layout.tsx — so bare `/` is still a Next fallback 404; US-005 may want a root redirect.
---

## 2026-04-19 - US-003
- Implemented: Verified static-export configuration meets all acceptance criteria. next.config.ts sets output: "export", trailingSlash: true, images.unoptimized: true. Both [locale] layout and [locale]/skills/[id] page declare dynamicParams=false with generateStaticParams enumerating all supported locales and skill IDs. Build emits out/ with index.html, /en, /ar, /en/skills, /ar/skills, /en/skills/saudi-zatca-phase2, /ar/skills/saudi-zatca-phase2, /en/generate, /ar/generate, favicon, _next/ assets, plus 404.html. Served out/ via pnpm serve and curl-probed /, /en/, /ar/, /en/skills/, /ar/skills/, /en/skills/saudi-zatca-phase2/, /en/generate/ — all returned 200. No code changes required.
- Files changed: ralph/prd.json (marked US-003 passes=true), ralph/progress.txt
- Checks run: pnpm verify (generate skills, doctor:react, schema typecheck, web typecheck, vitest, next build with static export) — all green. Manual static-serve probe — all 200.
- Notes for future work: Root index (/) currently serves Next's default app landing because the top-level app/page is inherited from root; acceptance only requires locale routes to work, and they do. A future story (US-004 / US-005) may want a root redirect to /en or a locale-picker index; track there. Still no root app/layout.tsx at apps/web/app — only [locale]/layout.tsx — which is why root / resolves via Next's fallback; acceptable for static hosting where /en/ and /ar/ are the canonical entry points.
---

## 2026-04-19 - US-002
- Implemented: Verified apps/web Next.js App Router scaffold meets acceptance criteria — app/ at package root (not src/), Tailwind v4 via @tailwindcss/postcss, ESLint via eslint-config-next flat config, package filterable as @agent-skills/web, locale-aware routing at app/[locale]/, and next.config.ts configured for static export (output: "export", trailingSlash, images.unoptimized). No code changes required.
- Files changed: ralph/prd.json (marked US-002 passes=true), ralph/progress.txt
- Checks run: pnpm verify — all green; build emits static pages for /en, /ar, /en/skills, /ar/skills, /en/generate, /ar/generate, and dynamic skill detail routes.
- Notes for future work: US-003 (confirm static out/ served locally), US-004 (locale routing already present — formalize lang/dir html attrs), US-005+ still open. Package names still use @agent-skills/* scope; rebranding to wathba is cosmetic and not blocking any acceptance criteria.
---

## 2026-04-19 - US-001
- Implemented: Verified pnpm workspace foundation is in place with root scripts (dev, build, lint, typecheck, test, generate:skills, verify, verify:full), shared tsconfig.base.json, and .gitignore covering Next.js/node_modules/dist/.turbo/coverage/playwright/test-results. No code changes required.
- Files changed: ralph/prd.json (marked passes=true), ralph/progress.txt
- Checks run: pnpm verify (skill generation, eslint doctor:react, schema typecheck, web typecheck, vitest unit tests, next build with static export output) — all green.
- Notes for future work: Root package.json still uses name "agent-skills"; not required by acceptance criteria but could be aligned with "Wathba Skills" branding later. Next priorities: US-002 (scaffold apps/web) — partly done; US-003 (static export) — next.js config already has output: export per generated pages in build log; confirm on next story. US-004/005/008/009/011/015/017/021/023/024/025/026/027/028/029/030/033/034/035/036/037/038/039/041/044/045/046/049/050 all still priority 1.
---
