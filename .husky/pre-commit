#!/usr/bin/env sh
# Fast governance gate — catches stale generated output and missing version
# bumps before a commit is created. For the full typecheck/test/build pipeline,
# run `pnpm verify` manually or rely on CI.
pnpm verify:skills
